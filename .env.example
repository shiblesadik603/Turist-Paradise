# Only used by docker-compose.yml — per-package dev servers use
# server/.env and client/.env instead. Copy to .env and fill in.

JWT_SECRET=
SSLCOMMERZ_STORE_ID=
SSLCOMMERZ_STORE_PASSWORD=
VITE_OPENWEATHER_API_KEY=
VITE_GEMINI_API_KEY=
